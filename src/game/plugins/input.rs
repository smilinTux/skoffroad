use bevy::prelude::*;

/// Minimal InputPlugin for plugin registration
pub struct InputPlugin;

impl Plugin for InputPlugin {
    fn build(&self, _app: &mut App) {
        // Add input systems here
    }
}
