use bevy::prelude::*;

/// Minimal EffectsPlugin for plugin registration
pub struct EffectsPlugin;

impl Plugin for EffectsPlugin {
    fn build(&self, _app: &mut App) {
        // Add effects systems here
    }
}
